spring:
  datasource:
    #?????????
    driver-class-name: com.mysql.cj.jdbc.Driver
    #?????url
    url: jdbc:mysql://${SPRING_DATASOURCE_URL:127.0.0.1:3306/content_global_translate}
    #??????
    username: ${SPRING_DATASOURCE_USERNAME:root}
    #?????
    password: ${SPRING_DATASOURCE_PASSWORD:Luke199182}
    hikari:
      #????????????250????????????30?
      connection-timeout: 2000
      #??????????10???0???maximum-pool-size??????maximum-pool-size
      minimum-idle: 10
      #??????????0????????10??????1?????minimum-idle??
      maximum-pool-size: 20
      #??????????????600000?10????????max-lifetime?max-lifetime>0??????0????0???10???????10??
      idle-timeout: 200000
      #?????????????????? true
      auto-commit: true
      # ????????????0???30??????????30??.?????mysql????????
      max-lifetime: 1800000
      #???????????????
      connection-test-query: select 1
    tomcat:
      jmx-enabled: true
  # 使用虚拟线程提升高并发下的吞吐与资源利用
  threads:
    virtual:
      enabled: true
mybatis:
   mapper-locations: classpath:mapper/**.xml
   configuration:
     map-underscore-to-camel-case: true
     # 使用 SLF4J 日志以兼容 Native Image
     log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl